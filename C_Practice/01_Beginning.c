#include "01_Beginning.h"

int main() {
	num = 1;
	printf("test");
	return 0;
	/*
	F9 breakpoint
	F5 If already generated exe file, it just run it, otherwise generate it and run
	F10 step over
	F11 step in
	Release: smailer, do optimization, faster
	Debug: bigger, less optimization, with debug info
	If find header file in current directory, using "", else <>
	*/
}